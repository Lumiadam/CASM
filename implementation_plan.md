# CASMS 實作計畫與執行紀錄

最後更新：2026-08-02

## 目前系統狀態

- 前端為 Next.js，已部署至 Cloudflare；後端與資料庫已部署至 Render。
- 本機 Docker Compose 已可啟動 PostgreSQL、後端、前端與 Nginx；`/api/health` 回傳資料庫已連線。
- 保留展示性資料與既有資料，不在目前或後續展示階段停用。
- 現有權限為 Admin、Custodian、Employee；Admin 審核中心僅限 Admin，其他角色僅能檢視自己的資產申請。

## 已完成的 CRUD 基礎

| 範圍 | 現況 |
| --- | --- |
| 資產 | Admin 可新增、編輯、封存資產，並調整分類與保管人。 |
| 預約 | Admin 可新增、編輯、調整預約狀態；今日以前的借用以「後期添加補件申請」標示。 |
| 資產申請 | Employee/Custodian 以名稱、編號、類型、分類提出申請；Admin 審核後可建立或更新資產。 |
| 權限 | Admin 可取得使用者清單並指派資產保管人；後續仍需完成完整的使用者角色調整介面與稽核。 |
| 維護 | 目前僅有基本維護紀錄與損壞紀錄，尚未形成完整的保固、報修、零件與結案流程。 |

## 下一階段目標（規劃中，不進行大規模更新）

本階段先完成資料模型、展示資料、CRUD 缺口與保修申報流程的設計確認，再分批實作。新增種子資料必須可重複執行、不可覆寫既有展示資料，並能補齊既有資料庫；不再以「已有 Admin 即停止」作為唯一種子判斷。

### 1. 展示資料擴充設計

#### 使用者與情境資料

- 保留目前的 Admin、Custodian、Employee 展示帳號與既有資料。
- 增加至少 1 名協助管理員、2 名不同職責的 Custodian（器材／空間設施）、5 名不同部門 Employee。
- 每個角色均配置可驗證權限的情境：可用、待審核、已核准、過期補件、維修中、保固中與零件更換紀錄。

#### 器材（可移動）

| 類別 | 展示資料與識別 | 預約規則 |
| --- | --- | --- |
| 微型密錄器 | 10 個獨立資產，料號 `REC-001` 至 `REC-010` | 每個資產獨立借用、獨立維護與零件紀錄。 |
| 三合一金屬探測器 | 2 個獨立資產，料號 `DET-001`、`DET-002` | 每個資產獨立借用、獨立維護與零件紀錄。 |
| 螢光背心 | 1 個庫存型資產，代碼 `VEST-STOCK-001`，總量 30 | 預約需輸入數量，依時段扣除可借數量。 |
| 無線麥克風 | 1 個庫存型資產，代碼 `MIC-STOCK-001`，總量 3 | 合併現有無線麥克風展示資料；預約需輸入數量。 |

每項器材加入保固起訖、建議保養日、維護／保修／零件更換歷程與保管人；庫存型資產不建立虛構序號。

#### 空間（不可移動）

| 空間 | 建議資產代碼 | 區分方式 |
| --- | --- | --- |
| 健身房 | `SPC-GYM-01` | 以所在位置識別。 |
| 會議室 3 間 | `SPC-MTG-01` 至 `SPC-MTG-03` | 各自編號與位置。 |
| 接待室 2 間 | `SPC-REC-01`、`SPC-REC-02` | 各自編號與位置。 |
| 開放式工作區 | `SPC-WORK-01` | 作為砂輪機所在位置。 |

空間應記錄位置、容納人數、維護紀錄，並能顯示所綁定的不可移動設施。空間的預約不因附屬設施故障自動停用，僅在詳情與預約提示中顯示警示備註。

#### 設施／設備

| 類別 | 規劃數量與代碼 | 關聯與預約規則 |
| --- | --- | --- |
| 冷氣主機 | 暫定 7 台，`HVAC-001` 至 `HVAC-007` | 分別綁定健身房、3 間會議室、2 間接待室、開放式工作區；經由空間預約，不可直接借用。 |
| 互動式電子白板 | 暫定 4 台，`IWB-001` 至 `IWB-004` | 綁定 3 間會議室與健身房；經由空間預約，不可直接借用。 |
| 移動式白板 | 2 座，`MWB-001`、`MWB-002` | 可直接預約，各自為獨立資產。 |
| 工業砂輪機 | 1 台，`GRD-001` | 固定於開放式工作區，但可直接預約。 |

每個設施紀錄可移動性、空間關聯、保固、預期汰換年限、維護／保修／零件更換歷程。上述冷氣與白板數量為展示規劃假設，實作前可依實際場域調整。

### 2. 必要的資料模型與預約規則

1. 建立可重複執行的 `seed_demo_data`：以固定代碼作為補齊依據，僅新增或更新受管理的展示資料，保留人工建立與既有展示內容。
2. 資產明確區分 `單件型` 與 `庫存型`：新增總量欄位與預約數量欄位；單件型固定數量為 1。
3. 預約衝突檢查改為依重疊時段合計「待審核／已核准」的庫存預約數量，不得超過可借總量；取消、拒絕、結束的預約不占用數量。
4. 建立 `space_id` 與 `is_movable` 關聯，固定設施可顯示於空間詳情但不等同空間本身的可借狀態。
5. 以正式欄位儲存分類、位置、保固與數量等可查詢資料；`metadata_json` 僅保留彈性展示欄位，避免核心規則依賴自由文字 JSON。

### 3. CRUD 盤點與改善方向

| 領域 | 目前缺口 | 下一步改善 |
| --- | --- | --- |
| 資產分類 | 分類資訊尚未成為一致的可管理資料 | 提供 Admin 分類 CRUD、封存保護與分類使用數量提示。 |
| 資產 | 單件、庫存、空間、固定設施共用模式不足 | 用類型／可移動性／庫存量／空間關聯決定可操作欄位與預約方式。 |
| 預約 | 尚無數量輸入、空間附屬設備提示與完整取消流程 | 表單依資產類型顯示數量、時段、用途、附屬故障提示、取消原因與補件註記。 |
| 保管與權限 | 使用者角色調整與異動歷程尚未完整呈現 | Admin 提供角色／部門／保管範圍調整，所有權限與保管異動寫入稽核紀錄。 |
| 申請／審核 | 已能審核資產申請，尚未統一預約與維護相關審核 | 統一審核待辦、決定原因、處理人與時間軸；一般使用者僅見自身紀錄。 |
| 封存 | 已能封存資產，關聯資料與復原規則待明確 | 封存前提示有效預約／未結案工單；支援 Admin 復原或保留封存原因。 |

### 4. 保固、維護與報修流程設計

建議以「報修工單」作為維護核心，並與資產、空間、零件和保固資訊連結。

```text
使用者／保管人報修
  → 待 Admin 審核
  → 保固處理／維修中／等待零件
  → 修復驗證
  → 結案（保留成本、零件、維修商與歷程）
```

建議工單狀態：`REPORTED`、`PENDING_APPROVAL`、`IN_WARRANTY`、`IN_REPAIR`、`WAITING_PARTS`、`VERIFICATION`、`CLOSED`、`CANCELLED`。

工單欄位至少包含：資產、影響空間、問題類型、嚴重度、描述、申報人、照片或連結、保固狀態、維修商、預估／實際成本、處理期限、結案驗證人與時間。

零件紀錄作為工單子項目，記錄零件名稱、料號、數量、供應商、成本、替換前後資訊與更換日期；資產另保留購置日、保固到期日、預計汰換日與下次保養日。

介面呈現原則：

- 資產詳情：狀態標籤、保固倒數、下一次保養、最近工單及零件更換時間軸。
- 空間詳情與預約表單：顯示固定設備及其故障警示；除空間本身停用外仍可預約。
- 使用者端：僅能建立與追蹤自己提出的報修／申請。
- Custodian：可補充診斷、維護紀錄與驗證結果。
- Admin：可審核、指派、調整狀態、成本與結案，並保留完整稽核紀錄。

### 5. 分階段執行順序

1. **規格確認**：確認 HVAC／白板實際數量、各空間位置、展示帳號命名與哪些資產可直接預約。
2. **資料庫與 API**：新增庫存數量、預約數量、空間綁定、可移動性、保固與工單所需 migration、驗證規則與權限測試。
3. **可重複展示資料**：實作 idempotent 種子資料，建立上述帳號、資產、空間、設施、預約、維護、故障與零件情境。
4. **CRUD 介面補齊**：先完成 Admin 分類／資產／預約／角色與保管設定，再完成一般使用者的友善數量預約表單。
5. **報修與維護介面**：完成申報、審核、工單看板、資產／空間詳情警示與維護時間軸。
6. **驗證與部署**：加入庫存衝突、權限隔離、封存、保固與工單流程測試；本機 Docker Compose 驗證後再部署 Cloudflare／Render。

## 已知驗證紀錄

- 後端 SQLite smoke test 已通過資產申請、審核、Admin 資產／預約 CRUD 與封存情境。
- 前端 `npm run build` 已成功，包含 Admin 管理頁與資產申請頁。
- Docker Compose 服務已健康，`http://localhost/api/health` 回傳 `200` 與資料庫連線成功。

## 系統基線索引（供後續 AI agent 檢視）

### 前端

- `frontend/app/dashboard/page.tsx`：資產、空間、設施瀏覽與預約入口。
- `frontend/app/admin/management/page.tsx`：Admin 資產與預約 CRUD。
- `frontend/app/maintenance/page.tsx`：報修建立與工單追蹤。
- `frontend/app/change-requests/page.tsx`、`frontend/app/admin/change-requests/page.tsx`：資產申請與 Admin 審核。
- `frontend/lib/api.ts`：前端 API 型別契約；變更 API 時必須同步更新。

### 後端與資料庫

- `backend/app/models.py`：SQLAlchemy 資產、預約、維護、工單與零件模型。
- `backend/app/seed.py`：`migrate_schema()` 只新增遺漏欄位；`run_seed()` 以固定代碼增補展示資料，不刪除既有資料。
- `backend/app/routers/assets.py`：資產、空間與固定設施 CRUD／顯示警示。
- `backend/app/routers/reservations.py`：預約 CRUD、時段庫存數量驗證、審核、取消與歸還。
- `backend/app/routers/maintenance.py`：基本維護紀錄、報修工單、工單狀態與零件 API。
- `backend/app/main.py`：啟動時依 `SEED_ON_STARTUP` 執行安全 migration 與展示資料補齊。

### 資料完整性規則

- 不重建、不清空、不覆寫既有資料庫；部署升級只執行新增欄位與新增固定代碼展示資料。
- 單件型資產的 `quantity_total=1`；庫存型資產以 `reservation_quantity` 進行同時段加總檢查。
- 固定設施以 `space_id` 綁定空間；設施維修警示不自動阻擋空間預約。
- 工單與零件皆關聯資產，保留申報人、處理狀態、成本、供應商與時間。

## 2026-08-02 下一階段執行紀錄

- [完成] 建立上述系統基線索引與資料完整性規則。
- [完成] 資產模型新增可移動性、庫存總量、位置、空間綁定、保固、汰換與下次保養欄位。
- [完成] 預約模型新增借用數量，改以同時段有效預約加總檢查庫存。
- [完成] 新增報修工單與零件更換模型／API，具完整狀態流程。
- [完成] 種子資料改為可重複增補，包含指定器材、空間、設施、帳號、預約、保養與待料情境。
- [完成] 前端已補上庫存數量預約輸入；Admin／Custodian 可在工單頁更新維修、待料、驗證與結案狀態。
- [完成] 後端 smoke test（3 項）與前端 production build（13 個路由）通過。
- [完成] Docker Compose 已重建並通過 `http://localhost/api/health` 健康檢查（HTTP 200，資料庫已連線）。

### 本次實作備註

- 容器第一次啟動時發現展示種子資料的砂輪機預約狀態被錯置到數量欄位，已改為具名 `approval` 參數；修正後資料庫啟動與健康檢查正常。
- `bcrypt`／Passlib 仍會在測試中輸出相容性警告，但不影響登入、測試結果或容器健康；可列為相依套件更新的後續維護項目。

## 2026-08-02 保修流程修訂

1. 缺失發現：使用者在歸還時填寫缺失與可用性；Admin／Custodian 可在所屬資產看板調整資產屬性與預約鎖定。
2. 預約鎖定：鎖定或封存資產仍出現在預約清單，右上角顯示圖示；前後端皆以訊息阻擋建立預約。
3. 報修處理：僅 Admin 與 Custodian 可建立及處理工單；Custodian 僅限自己保管資產，Admin 可處理全部資產。
4. 可用性對照：`USABLE`（可使用）→ 舊記錄 `LOW`、`PARTIALLY_USABLE`（部分可使用）→ `MEDIUM`、`UNUSABLE`（不可使用）→ `HIGH`；保留舊欄位以維持既有資料相容。

### 實作完成狀態

- [完成] `my-assets` 看板：Admin 可查看全部、Custodian 僅查看自己保管的資產，並可調整名稱、位置、下次保養日與預約鎖定。
- [完成] 歸還缺失表單改為「可使用／部分可使用／不可使用」，後端保留相容對照以避免破壞舊紀錄。
- [完成] 預約頁保留鎖定／封存資產並以圖示顯示；前端提示與後端 API 均阻擋建立預約。
- [完成] 報修建立與工單處理僅開放 Admin／Custodian，Custodian 的後端權限限制於保管資產。
