# CASMS Docker 環境變數範例（複製為 .env 後調整）

POSTGRES_USER=casms
POSTGRES_PASSWORD=casms_secret
POSTGRES_DB=casms

DATABASE_URL=postgresql://casms:casms_secret@postgres:5432/casms
SECRET_KEY=change-me-in-production
SEED_ON_STARTUP=true

NEXT_PUBLIC_API_URL=/api
